# Implementation Plans

Plans 001–010 generated by the improve advisor on 2026-07-11 / 2026-07-17 and all DONE. Plans 011–021 generated on 2026-07-20 (HEAD `72819cd`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix Frontend Vitest Harness and Test Execution | P1 | S | None | DONE |
| 002  | Add Biome Lint, Format, and Typecheck Configuration | P1 | S | 001 | DONE |
| 003  | Fix Loop Detector False Positive on Successful No-Op Results | P1 | S | 002 | DONE |
| 004  | Close Shell Flag-Style Path Escape | P1 | S | 003 | DONE |
| 005  | Route Skill Script Execution Through Approval Broker | P1 | M | 004 | DONE |
| 006  | Close Whitespace-Containing Shell Path Bypass | P1 | M | None | DONE |
| 007  | Harden Skill Script Directory Sandbox | P1 | S | None | DONE |
| 008  | Harden Corpus Symlink Boundaries | P1 | M | None | DONE |
| 009  | Fix Local Session Streaming Fallback | P1 | S | None | DONE |
| 010  | Bound Subprocess Output Capture | P1 | M | 006, 007 | DONE |
| 011  | Persist TopBar Inline Session-Title Edits | P1 | S | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-011`, branch `advisor/011-persist-session-title`, tips `7325a30`/`8bd20b4`/`f288d90` |
| 012  | Characterization Tests for `useChatStream` (Streaming Foundation) | P1 | M | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-012`, branch `advisor/012-stream-tests`, tip `df3a05e` (11 characterization tests across 3 describe blocks) |
| 013  | Gate Workspace Switcher + TopBar Switch Button While Streaming | P1 | M | 012 | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-013`, branch `advisor/013-gate-workspace-switch`, tips `540f3ce`/`f1b6fec` (based on 012 tip `df3a05e`; 2 new race tests) |
| 014  | Cancel Streaming + Per-Run Event Isolation | P1 | M | 012 | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-014`, branch `advisor/014-cancel-stream-isolation`, tips `634c8e7`/`4e39873`/`f123f10` (based on 012 tip `df3a05e`; 5 new FE tests + 4 new BE tests; `Abortable`-based cancel + `runId` per-run isolation) |
| 015  | Delete Dead/Duplicate Components and Misleading Tests | P2 | S | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-015`, branch `advisor/015-delete-dead-components`, tip `112e552` (−1,450 LOC; 6 deletions + 2 edits; test count 116→112) |
| 016  | Hard-Block `rm --recursive /` and Long-Form Destructive Patterns | P1 | S | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-016`, branch `advisor/016-rm-recursive-hardblock`, tip `e771335` (10 new tests) |
| 017  | Surface Resolved Skill-Script Interpreter in Approval Request | P1 | S | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-017`, branch `advisor/017-skill-interpreter-approval`, tip `3adc627` (6 new tests; pure `build_script_approval_label` seam) |
| 018  | Isolate Untrusted Content in Verification/Review Prompts | P1 | M | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-018`, branch `advisor/018-prompt-injection-isolation`, tip `68e94e1` (7 new tests; `wrap_untrusted` helper + post-parse downgrade validator) |
| 019  | Harden Trace Log Redaction for Free-Form Strings and Pretty-Printed JSON | P2 | S | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-019`, branch `advisor/019-trace-redaction-hardening`, tip `1ccaa9c` (6 new tests; whitespace-tolerant JSON + Bearer/sk-/ghp_/gho_/ghs_/sk-ant- token patterns via existing `regex` crate) |
| 020  | Migrate `serde_yaml` → `serde_yml` on the Skill-Frontmatter Parse Path | P2 | S | None | REJECTED — `serde_yml` is itself deprecated (shim to `noyalib`); the plan's factual premise is wrong. Re-plan against `noyalib` (with a Step 0 API-surface check) if/when the migration becomes urgent. `serde_yaml 0.9.34+deprecated` is not causing active harm (single call site, thick test coverage, no known CVE). |
| 021  | (D1 Spike) Investigate Surfacing `.gospel/PLAN.md` as a First-Class UI Panel | P2 | M | None | DONE — worktree `/var/folders/k0/0016wr394xv8g7k5630n2tbw0000gn/T/opencode/gospel-exec-021`, branch `advisor/021-plan-panel-spike`, tips `90a7d51`/`c919288` (6 parser tests + debug-gated PlanPanel + Spike Findings with Open Questions A/B) |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with one-line reason) | `REJECTED` (with one-line rationale)

## Dependency notes

- Plans 001–010 dependency notes remain as previously recorded.
- **013 depends on 012**: The workspace-streaming-leak refactor needs the streaming characterization tests to exist as the regression net.
- **014 depends on 012**: Cancel + per-run event isolation refactors `useChatStream.ts` listeners; plan 012's tests are the safety net. Plan 014 updates the `TODO(plan 014)` test marker left by plan 012.
- **011, 015, 016, 017, 018, 019, 020, 021 are independent** and can start in any order. They touch disjoint files; the suggested execution order (011 first) is purely leverage-weighted, not structural.
- **015 may land alongside any plan in 011–014** — it touches deleted components only, plus a small `SessionDrawer.tsx` edit; merge friction with 011+013 is minimal.

## Execution order suggestion

Recommended order for a single executor working sequentially:

1. **011** (TopBar title persistence) — cheap, independent, high user-impact.
2. **012** (streaming characterization tests) — foundation; gates 013/014.
3. **016**, **017** — small independent backend security fixes; batchable.
4. **018** (prompt-injection isolation) — larger; do after 016/017 to avoid context-switching backend→frontend→backend.
5. **019**, **020** — small backend hardening/migration; batchable.
6. **013** (workspace-switch guard) — needs 012 landed.
7. **014** (cancel + per-run isolation) — needs 012 landed; updates 012's TODO marker.
8. **015** (delete dead components) — anywhere; cheap; somewhere mid-stream to clear the bundle.
9. **021** (D1 spike) — anywhere; investigation, not blocking.

Parallel executors: 011, 015, 016, 017, 018, 019, 020, 021 are all independent; 013 and 014 wait for 012.

## Findings considered and rejected (this run, 2026-07-20)

The 2026-07-20 audit produced the following findings that were considered but NOT turned into plans. They are recorded so the next audit can skip them without re-investigating:

- **Skill-script env-key denylist for custom MCP servers (SEC-05)**: Narrow threat model — the user already configures their own MCP server; threat surface limited. Re-flag only if multi-tenant/server-sharing becomes supported.
- **GitHub Copilot OAuth tokens mode enforcement (SEC-01)** — *deferred, not rejected*: marked at P1 in prior audit but the writer (rig) controls umask, not gospel. Logged in the vetting notes; recommend a separate small plan once the rig provider integration is documented in an ADR.
- **`env -S` argv split mismatch (SEC-07)**: MED confidence; the classifier never actually launches `env -S` so the impact is a classification discrepancy, not a real spawn. Deferred until `env`-wrapped commands surface in user telemetry.
- **Quote-containing `argument_path_value` escape (CORR-02)**: LOW confidence; most tools don't strip surrounding quotes. Logged for awareness.
- **`truncate_text_bytes` tiny-cap seam (CORR-03)**: Latent only; all current callers pass ≥ KiB caps. Re-flag if a per-line caller is added.
- **`SessionStore::get_archive_policy(None)` mis-reports `uses_workspace_override` (CORR-04)**: MED confidence; no frontend currently gates on the flag, so impact is latent. Re-flag when an archive-stats UI lands.
- **ReviewerPanelCard smooth-scroll without `prefers-reduced-motion` (FE-CORR-06)**: Small a11y screen; recommend folding into a future a11y pass with `CORRECTNESS-05` (aria-live nesting) and `CORRECTNESS-08` (side-stripe `!important`). Not worth a standalone plan.
- **Nested `aria-live` regions (FE-CORR-05)** and **DESIGN.md side-stripe anti-pattern in production CSS (FE-CORR-08)**: Bundled into the deferred a11y-and-design-drift pass above.
- **`dead package-lock.json` cleanup (TECH-DEBT-01)** and **dead `greet` template command (TECH-DEBT-02)**: Cheaper than a plan — fold them into the next maintainer's opportunistic cleanup commit. Re-flag if they persist past the next routine sweep.
- **`rusqlite` 0.32 → 0.40 migration (DEPS-03)** and **`tree-sitter` 0.23 → 0.26 migration (DEPS-04)**: Deferred until a transitive dep forces them or a needed feature lands. NOT recommended as standalone plans now (high effort, low product leverage).
- **Bun CI cache (DX-02)**: Small savings (Bun install is already ~seconds); re-flag if CI install time becomes a measured bottleneck.
- **README replacement (DX-01)**: Real friction, recommended, but flagged as a maintainer-quality-of-life edit rather than engineering leverage. Re-flag if onboarding friction is reported.
- **Lint noise from throwaway prototype (TECH-DEBT-03)**: The prototype is explicitly throwaway and dev-gated; loosening its lint rules loses nothing but the savings are cosmetic. Re-flag if the prototype is promoted toward production.
- **`lib.rs` god-module split (TECH-DEBT-04)**: NOT worth doing — the file is thin command wiring with no logic clusters; navigation by `rg "#\[tauri::command\]"` already lands on the right adapter. Reject as a standalone refactor; only opportunistically extract when adding a cluster of related commands.
- **SessionDrawer/ReviewResults virtualization (PERF-01)**: Real performance finding but DESIGN.md-mandated; recommend a dedicated plan only after the dead-review-component cleanup (015) lands and the live review surface is stable. Re-flag then.
- **`useConstellation` / `ChatView` memoization (PERF-02)** and **AppShell session-reload storm (PERF-03)**: MED confidence re-render impact; defer until profiling shows measurable jank during streaming on long sessions.
- **Direction items D2–D6** (Context Panel / Multi-Agent View / Review Run Records history / Corpus user command / Onboarding+Settings reconciliation): Each is a separate spike candidate; recommend picking at most one per quarter since each is multi-day. D1 (PLAN.md panel) selected for this run as the most-grounded (the substrate + tool + CONTEXT.md rationale already exist).

## Findings considered and rejected (prior run, preserved)

- **CI/CD Test Automation Workflow**: Audited but rejected for the 2026-07-11 iteration to focus effort on local developer experience (DX) and security boundaries.
- **Stylelint Configuration**: Considered but rejected to focus purely on core JS/TS linting, formatting, and TypeScript compiler checks.
